# 项目更新日志

更新时间：2026-06-09

## 2026-06-09 文档边界整理

背景：本地工作区里同时存在当前主线、历史测试输出、旧平铺目录和过期打包副本，容易混淆“现在真正使用的文件”和“旧资料”。

已完成：

- 更新顶层文件地图：`CURRENT_PIPELINE_MAP_20260609.md`。
- 在当前素材管线包里新增：`README_当前主线.md`。
- 在当前素材管线包 `README.md` 顶部加入当前主线说明入口。
- 更新 `docs/场景资产生成扩展方案.md`，移除文档示例里的本机绝对路径。
- 更新 `分工文件/画面切换/README_当前窗口文件说明.md`，指向当前游戏仓库、当前视频素材管线和归档位置。
- 将 `分工文件/01_窗口_视频处理与素材生成/交付物/` 下旧平铺目录和过期可发送包归档到 `_archive_旧文件_20260609_短线整理/视频素材管线交付物旧平铺_20260609_边界整理/`。

保留边界：

- 当前真正使用的视频素材管线仍在 `分工文件/01_窗口_视频处理与素材生成/交付物/给游戏同学_视频转主题资产包/`。
- 当前真正运行的游戏仓库仍在 `Tap-Fight/`。
- 旧文件只移动到 archive，没有删除。

验证：

- 视频素材管线 `npm run validate` 通过。
- Tap-Fight client `npm run build` 通过。
- 扫描当前主线文档和代码，未发现本机绝对路径残留。

## 2026-06-09 GitHub 最新版隔离更新

目标：将 GitHub 最新版 `SpaceT1934/Tap-Fight` 克隆到独立目录后，只把“视频生成资产”相关内容更新进去，不影响本地当前工作仓库，也不向远程 push。

克隆位置：

```text
pull下来的地方/Tap-Fight
```

GitHub 最新提交：

```text
ad54907 Remove external URLs from READMEs
```

新仓库结构：

```text
pull下来的地方/Tap-Fight/
  Tap-Fight/
  video_theme_asset_pipeline/
```

已完成：

- 将本地当前视频素材管线的代码、配置、文档和入口脚本同步到新仓库 `video_theme_asset_pipeline/`。
- 复制内部开发工具页 `Tap-Fight/client/public/asset-pipeline.html`。
- 在新仓库 `Tap-Fight/server/src/main.ts` 中合并内部视频管线 API：
  - `POST /api/video-pipeline/jobs`
  - `GET /api/video-pipeline/jobs/:id`
  - `GET /api/video-pipeline/jobs/:id/asset?path=<asset_path>`
- 内部开发页增加 `生成场景资产` 开关，开启时 server 会给 `package_video.ps1` 传 `-GenerateStageAssets`。
- 导出到 Tap-Fight 运行包时，生成场景资产会同时写入主题根目录和 `assets/stage/`：
  - 根目录文件供当前游戏运行时读取。
  - `assets/stage/` 文件保持 ThemeAssetPackage 结构化路径完整。
- 保留 GitHub 最新版已有的排行榜、结算、语音技能和自动匹配逻辑。

明确未做：

- 没有在游戏菜单或玩家入口增加“视频生成资产”按钮。
- 没有改变游戏玩法、联机、伤害判定或结算逻辑。
- 没有复制运行输出目录 `theme_packs/`、`video_understandings/` 或网页上传缓存。
- 没有执行 `git push`。

当前内部工具页定位：

- `asset-pipeline.html` 是开发/演示用后端工具页，不面向最终玩家。
- 需要直接访问页面地址使用，不从游戏菜单暴露入口。

## 下一步待处理

下一步需要处理“资产生成结果和游戏运行目录的文件对接”：

- 原有流程已经支持 ThemeAssetPackage 生成后导入 `Tap-Fight/client/public/theme_packs/<theme_id>/`。
- 本轮视频管线新增了可选生成资产：
  - `assets/stage/background.png`
  - `assets/stage/platform_left.png`
  - `assets/stage/platform_mid.png`
  - `assets/stage/platform_right.png`
  - `assets/stage/hazard_debris.png`
- 已确认导出到游戏运行目录时，这些生成资产会优先覆盖模板资产；如果缺失或未开启生成，则继续回退 `StageTemplate`。
- 内部网页工具页和 CLI 都已经具备 `GenerateStageAssets` 开关；默认关闭，开启后才生成场景资产。

## 2026-06-09 楚人美真实生成与游戏导入验证

目标：在隔离的新仓库 `pull下来的地方/Tap-Fight/` 中，用楚人美视频真实跑通“视频理解 -> 角色/投掷物/技能语句 -> 可选场景资产 -> ThemeAssetPackage -> Tap-Fight 运行目录”的完整链路。

生成主题：

```text
real_churenmei_stage_20260609_201010
```

输入视频：

```text
分工文件/01_窗口_视频处理与素材生成/视频源素材/楚人美游记 P1 14-火云洞-上.mp4
```

运行方式：

```powershell
.\package_video.ps1 `
  -Video "<楚人美视频路径>" `
  -ThemeId "real_churenmei_stage_20260609_201010" `
  -GenerateStageAssets `
  -DrawConcurrency 2 `
  -DrawRetries 3 `
  -DrawTimeoutMs 300000 `
  -ExportTapFight `
  -TapFightDir ".\Tap-Fight" `
  -StageTemplate "office_battle_001"
```

结果：

- 视频理解缓存已生成并复用：`video_understandings/understanding_p1_14_63f72941/`。
- ThemeAssetPackage 输出：`video_theme_asset_pipeline/theme_packs/real_churenmei_stage_20260609_201010/`。
- Tap-Fight 运行目录输出：
  - `Tap-Fight/client/public/theme_packs/real_churenmei_stage_20260609_201010/`
  - `Tap-Fight/client/public/videos/real_churenmei_stage_20260609_201010.mp4`
  - `Tap-Fight/client/public/theme_registry.json`
- 角色：`猴妖` vs `红衣少年`。
- 武器/投掷物：`金箍长棍` vs `三昧火焰枪`，远程投掷物归属 `p2`。
- 技能语句：
  - `p1`: `你不要骗我`
  - `p2`: `管她呢赶紧下锅`
  - 主题吐槽：`比六丁圣火还好`
- `can_load_in_game: true`。
- `can_load_in_tap_fight: true`。

场景资产：

- `assets/stage/background.png`，尺寸 `1080x1920`。
- `assets/stage/platform_left.png`，尺寸 `58x54`。
- `assets/stage/platform_mid.png`，尺寸 `128x54`。
- `assets/stage/platform_right.png`，尺寸 `58x54`。
- `assets/stage/hazard_debris.png`，尺寸 `48x48`。
- 导出时同步保留游戏根目录兼容文件：`background.png`、`platform_left.png`、`platform_mid.png`、`platform_right.png`、`hazard_debris.png`。

生成过程记录：

- 第一次使用较高生图并发时，Right Codes draw 曾返回 `HTTP 524` 上游超时。
- 后续使用 `DrawConcurrency 2`、`DrawRetries 3` 成功跑完。
- 这说明问题是 draw 服务瞬时超时/并发压力，不是本地包结构或 Tap-Fight 导入链路损坏。

平台白边修复：

- 发现生成平台图存在白色实底，游戏里会看到明显白边。
- 处理位置限定在视频资产生成管线：`video_theme_asset_pipeline/video-pipeline/tools/stage_asset_postprocess.py`。
- 没有修改 Tap-Fight 游戏代码。
- 修复方式：对透明类 stage asset 做“边界连通近白背景”清理，只处理从图片边缘连通进来的白底/浅灰底，保留平台主体和固定尺寸。
- 当前包已重处理并同步到新仓库 Tap-Fight 运行目录。

修复后抽检：

```text
platform_left.png   58x54   transparent 0.8630   white 0
platform_mid.png    128x54  transparent 0.8265   white 0.0001
platform_right.png  58x54   transparent 0.8298   white 0.0003
hazard_debris.png   48x48   transparent 0.6385   white 0
```

验证：

- `node --check video-pipeline/src/index.mjs` 通过。
- `python -m py_compile video-pipeline/tools/stage_asset_postprocess.py` 通过。
- `npm run validate` 通过。
- Tap-Fight client `npm run build` 通过。
- Tap-Fight server `npx tsc --noEmit --rootDir ..` 通过。
- 内部资产页可读取主题包：`http://127.0.0.1:5174/asset-pipeline.html?themeId=real_churenmei_stage_20260609_201010`。

边界纠正：

- 曾临时将本主题导入旧本地仓库 `Tap-Fight/` 以便旧 5173 页面查看；该操作不符合本轮“只处理 pull 下来的新仓库”的边界。
- 已清理旧仓库中的同名主题目录、同名视频和 registry 条目。
- 当前有效结果只保留在 `pull下来的地方/Tap-Fight/`。
- 没有执行 `git push`。
